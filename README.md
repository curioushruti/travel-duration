## Setup

```
bash setup.sh
```

Create env variables for API KEYS:

1. Copy `.env.example` to `.env`
2. Replace placeholder text with your own API keys

## Run the app

Activate python virtual environment:

```
source ./.travel-env/bin/activate
```

Run streamlit app:

```
streamlit run app.py
```
